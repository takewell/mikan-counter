extends layout

block content
  br
  if !user
    h1=title

  if user
    div(class="mx-auto text-center")
      br
      div
        h1 Mikan Count
        button
          img(src="/img/logo.png", alt="" style="width:200px;")
      br
      div#app